spring:
  config:
    import: >
      classpath:config/application-infraimatic.yml,
      classpath:config/application-twillio-test.yml,
      classpath:config/application-google-test.yml,
      optional:classpath:config/application-${spring.profiles.active}.yml
logging:
  level:
    org.flywaydb.core: INFO
    org.springframework.boot.autoconfigure.flyway: DEBUG
